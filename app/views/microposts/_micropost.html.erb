<tr>
<td class="micropost">
<span class="title"><%=micropost.title%></span>
<span class="content"><%=micropost.content%></span>
<span class="timestamp">Posted @ <%= time_ago_in_words(micropost.created_at)%> ago</span>
</td>
<td>
<% unless current_user.nil? %>
  <% if current_user.id == micropost.user.id %>
  <%= link_to "Delete", micropost, :method => :destroy, :confirm => "Are you sure?"%>
<% end%>
<% end %>
</td>

</tr>
